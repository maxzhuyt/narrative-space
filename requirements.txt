# Python dependencies for the narrative-continuation pipeline.
#
# Tested environment (NCSA DeltaAI, partition ghx4, conda env
# /projects/bgye/envs/llm):
#   Python 3.11.15
#   CUDA 12.9
#   ARM Grace-Hopper (GH200) / H100 96 GB HBM3
#
# The sbatch wrappers activate the pre-built env directly; this file is
# provided for reproducing the env elsewhere (e.g. local CPU-only analysis
# or a new cluster) via:
#
#   pip install -r requirements.txt
#
# For the exact on-cluster build, see
# /projects/bgye/envs/llm (`conda list` inside that env).

# ── Core runtime: vLLM + its deps ────────────────────────────────────────
# vllm pulls in a compatible torch; we pin torch separately to lock the
# CUDA 12.9 wheel explicitly on the cluster.
vllm>=0.19.1
torch>=2.10.0
transformers>=5.6.0

# ── HuggingFace hub (token auth + model-metadata checks) ─────────────────
huggingface_hub>=1.11.0
